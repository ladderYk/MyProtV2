// tests/Engine.Tests/ExpressionEvaluatorTests.cpp — 表达式求值器单元测试占位

#include "MyProt/Engine/ExpressionEvaluator.hpp"
#include "MiniTest.hpp"

// TODO: ExpressionEvaluator 测试 (resp[N] 引用, 比较运算, 算术运算)
// 优先级: || < && < | < ^ < & < == < << < + < * < 一元 (KI-13)

TEST(ExpressionEvaluatorTest, Placeholder) {
    SUCCEED();
}
